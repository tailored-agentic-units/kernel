services:
  ollama:
    image: ollama/ollama:latest
    container_name: kernel-ollama
    ports:
      - "11434:11434"
    volumes:
      - ~/.ollama:/root/.ollama
    environment:
      - OLLAMA_KEEP_ALIVE=5m
      - OLLAMA_HOST=0.0.0.0
    devices:
      - nvidia.com/gpu=all
    restart: unless-stopped
    entrypoint: ["/bin/bash", "-c"]
    command: >
      "ollama serve &
      sleep 10 &&
      echo 'Pulling qwen3:8b model...' &&
      ollama pull qwen3:8b &&
      echo 'Model ready. Ollama server running.' &&
      wait"
